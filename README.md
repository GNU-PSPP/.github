# .github
GNU PSPP keeps survey tables you own in one named syntax desk so frequencies stay easy to reverse on a host you already operate. PSPP Software keeps the listing honest, so PSPP stays a file you can still restore each day on that same folder without extra noise.
